/* tslint:disable:no-unused-variable */

import { DiscriptionPipe } from './discription.pipe';

describe('Pipe: Discriptione', () => {
  it('create an instance', () => {
    let pipe = new DiscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
